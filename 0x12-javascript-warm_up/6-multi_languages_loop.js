#!/usr/bin/node

// Script that prints multiple strings using a loop.

let i;
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (i = 0; i < languages.length; i++) {
  console.log(languages[i]);
}
